spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/matsinhe
    username: matsinhe
    password: matsinhe
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
  sql:
    init:
      mode: never